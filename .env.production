# Chargé automatiquement par `vite build` (mode production). Pas de secrets ici.
# Les clés Firebase peuvent rester dans .env.local au moment du build, ou être injectées en CI.

VITE_USE_FICTITIOUS_DATA=false
